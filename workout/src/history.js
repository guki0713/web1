// 시드(초기) 기록 — 비워 둔다.
//
// 개인 운동기록은 저장소에 커밋하지 않는다. 이 저장소는 공개되어 있고,
// 날짜별 훈련량은 개인 정보이기 때문이다. 기록은 각자의 브라우저에만 저장되며,
// 새 기기에서는 앱의 [데이터 → JSON 가져오기]로 백업 파일을 한 번 불러오면 된다.
//
// 백업 파일은 [데이터 → JSON 내보내기]로 언제든 만들 수 있고,
// Numbers 파일에서 새로 뽑으려면 다음을 쓴다(결과 JSON은 커밋하지 말 것):
//   python3 tools/convert_numbers.py 2026운동일지.numbers -o 백업.json
window.WORKOUT_HISTORY = [];
